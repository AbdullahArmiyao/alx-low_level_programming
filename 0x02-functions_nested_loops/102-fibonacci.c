#include <stdio.h>

/**
 * main -Entry point
 *
 * Return: void
 *
 */

int main(void)
{
	int c, f1, f2, s;

	for (c = 0; c < 50; c++)
	{
		s = f1 + f2;
		printf("%d", s);

		f1 = f2;
		f2 = sum;

		if (c == 49)
			printf("\n");
		else
			printf(", ");

	return (0);
}
